import {createTheme} from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#3a37ee'
        },
        secondary: {
            main: '#76ffb5'
        },
        error: {
            main: '#ec0f2a'
        }
    }

})

export default theme;